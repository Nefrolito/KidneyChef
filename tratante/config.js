// Configuración del portal del tratante.
//
// SUPABASE_ANON_KEY es pública por diseño (pensada para vivir en el cliente,
// protegida por RLS del lado de Supabase) — no es un secreto, igual que la
// APP_KEY de la app del paciente. La SERVICE_KEY nunca debe aparecer acá.
//
// Completar estos tres valores cuando esté creado el proyecto de Supabase
// (ver checklist de la Fase 0 en el plan) y, si el backend no vive en el
// mismo origen que este portal, la URL donde corre server.py.
const SUPABASE_URL = "";
const SUPABASE_ANON_KEY = "";
const API_BASE = "";
